class WebtoonModel {
  final title, thumb, id;

  WebtoonModel.createmodel(
      {required String title, required String thumb, required String id})
      : title = title,
        thumb = thumb,
        id = id;
}
